name: BotDetection
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - tweepy
  - gensim
  - pandas
  - pytorch=1.0.1
  - torchvision==0.2.1
  - numpy
  - tqdm
  - matplotlib
#  # all pip dependencies
  - pip:
    - astar-tcn --no-dependencies
    - nltk
    - pywikibot
    - langdetect